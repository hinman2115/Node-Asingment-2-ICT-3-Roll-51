<!DOCTYPE html>
<html>
<head>
  <% include ../partials/head %>
</head>
<body>
<% include ../partials/nav %>
<main>
  <h2>New Employee</h2>
  <% if (errors) { %><p class="error"><%= errors %></p><% } %>
  <form action="/employees" method="POST">
    <div class="row"><label>Name</label><input name="name" value="<%= values.name || '' %>" required /></div>
    <div class="row"><label>Email</label><input name="email" type="email" value="<%= values.email || '' %>" required /></div>
    <div class="row"><label>Phone</label><input name="phone" value="<%= values.phone || '' %>" /></div>
    <div class="row"><label>Department</label><input name="department" value="<%= values.department || '' %>" /></div>
    <div class="row"><label>Basic</label><input name="basic" type="number" min="0" step="1" required value="<%= values.basic || '' %>" /></div>
    <div class="row"><label>HRA %</label><input name="hraPercent" type="number" min="0" step="1" value="<%= values.hraPercent || 20 %>" /></div>
    <div class="row"><label>DA %</label><input name="daPercent" type="number" min="0" step="1" value="<%= values.daPercent || 10 %>" /></div>
    <div class="row"><label>Deductions</label><input name="deductions" type="number" min="0" step="1" value="<%= values.deductions || 0 %>" /></div>
    <button type="submit">Create</button>
  </form>
  <p>Note: An autogenerated password will be <strong>emailed</strong> to the employee and stored hashed.</p>
</main>
</body>
</html>
